'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
var convertToSquareBoard = Object.defineProperty(Array.prototype, 'convertToSquareBoard', {
    value: function value() {
        try {
            var size = this.length;
            if (Math.sqrt(size) % 1 != 0) {
                throw new Error('convertToSquareBoard only works on arrays with a perfect square size! SIZE=' + size);
            } else {
                var width = Math.sqrt(size);

                var newMatrix = this.reduce(function (rows, key, index) {
                    return (index % width == 0 ? rows.push([key]) : rows[rows.length - 1].push(key)) && rows;
                }, []);

                return newMatrix;
            }
        } catch (e) {
            console.error(e);
        }
    }
});

exports.default = convertToSquareBoard;